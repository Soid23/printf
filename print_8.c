#include "main.h"
/**
 * print_8 - print octal numbers
 * @arg: argument passed
 * Return: count
 */
int print_8(va_list val)
{
	int b;
	counter = 0;

	unsigned int num = va_arg(val, unsigned int);

	while (num / 8 != 0)
}
